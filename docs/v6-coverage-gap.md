# Pine Script v6 — coverage gap report (piner vs the reference manual)

Auto-generated by `scripts/v6-coverage-audit.ts` (`bun scripts/v6-coverage-audit.ts`): a name-by-name diff of every documented `##` entry in the
bundled v6 reference manual against piner's implemented surface, grouped by the manual's 7 top-level
sections (Types, Variables, Constants, Functions, Keywords, Operators, Annotations).

> The bundled manual is an older v6 snapshot (no `type_footprint`); features TradingView added since
> are not visible here. See coverage-and-compatibility.md §3.1.

**846/884 documented entries covered (95.7%).**

## Variables — 128/160 covered · **0 fillable, 32 deferred**

**Deferred** (tick-level bid/ask (no L1 quote feed)): `ask`, `bid`

**Deferred** (forward fundamental data (no feed)): `dividends.future_amount`, `dividends.future_ex_date`, `dividends.future_pay_date`, `earnings.future_eps`, `earnings.future_period_end_time`, `earnings.future_revenue`, `earnings.future_time`

**Deferred** (session state (no session model)): `session.ismarket`, `session.ispostmarket`, `session.ispremarket`

**Deferred** (live exchange/fundamental metadata (no data feed)): `syminfo.current_contract`, `syminfo.expiration_date`, `syminfo.industry`, `syminfo.main_tickerid`, `syminfo.recommendations_buy`, `syminfo.recommendations_buy_strong`, `syminfo.recommendations_date`, `syminfo.recommendations_hold`, `syminfo.recommendations_sell`, `syminfo.recommendations_sell_strong`, `syminfo.recommendations_total`, `syminfo.sector`, `syminfo.shares_outstanding_float`, `syminfo.shares_outstanding_total`, `syminfo.target_price_average`, `syminfo.target_price_date`, `syminfo.target_price_estimates`, `syminfo.target_price_high`, `syminfo.target_price_low`, `syminfo.target_price_median`

## Constants — 204/204 covered ✅

## Functions — 451/457 covered · **0 fillable, 6 deferred**

**Deferred** (risk-limit controls (need broker halt logic)): `strategy.risk.allow_entry_in`, `strategy.risk.max_cons_loss_days`, `strategy.risk.max_drawdown`, `strategy.risk.max_intraday_filled_orders`, `strategy.risk.max_intraday_loss`, `strategy.risk.max_position_size`

## Keywords — 15/15 covered ✅

## Types — 18/18 covered ✅

## Operators — 20/20 covered ✅

## Annotations — 10/10 covered ✅

piner treats `//@…` as comments (ignored, no error); `//@version` is honored. Annotations carry no execution semantics, so this is functionally complete.

---

**0 fillable gaps** (no external data/infra needed) across Variables/Constants/Functions; the rest need a data feed or a larger subsystem.
